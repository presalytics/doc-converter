server {
    listen      DC_LISTEN_PORT;
    server_name DC_SERVER;
    charset     utf-8;
    client_max_body_size 75M;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/uwsgi.socket;
    }    
}